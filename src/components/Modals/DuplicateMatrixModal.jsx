import React from "react";
import Modal from "../../common/components/Modal/Modal";

const DuplicateMatrixModal = ({
  isOpen,
  toggleModal,
}) => {
  return (
    <Modal
      isOpen={isOpen}
      toggleModal={toggleModal}
    >
    
    </Modal>
  );
};

export default DuplicateMatrixModal;
